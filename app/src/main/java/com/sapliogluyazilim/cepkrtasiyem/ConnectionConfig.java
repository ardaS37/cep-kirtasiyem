package com.sapliogluyazilim.cepkrtasiyem;

/**
 * Deliberately contains no production credentials.
 * Use a secure backend API in production rather than embedding database access
 * details in an Android application.
 */
public final class ConnectionConfig {
    public static final String CONNECTION_URL = "";

    private ConnectionConfig() {
    }
}
